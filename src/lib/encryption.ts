/**
 * Stub encryption module — will be implemented when Plaid integration is added.
 */

export function encrypt(text: string): string {
  // Placeholder — real implementation will use AES-256-GCM
  return text;
}

export function decrypt(text: string): string {
  // Placeholder — real implementation will use AES-256-GCM
  return text;
}
